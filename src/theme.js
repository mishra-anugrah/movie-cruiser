import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    background: {
      paper: "#394B61",
    },
    text: {
      primary: "#D4D7DD",
    },
  },
  typography: {
    fontFamily: "Open Sans",
    fontWeight: 600,
    fontSize: "15px",
  },
});
